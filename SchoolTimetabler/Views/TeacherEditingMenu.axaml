<UserControl    x:Class="SchoolTimetabler.Views.TeacherEditingMenu"    xmlns="https://github.com/avaloniaui"    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"    xmlns:local="using:SchoolTimetabler"    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    xmlns:vm="clr-namespace:SchoolTimetabler.ViewModels"    d:DesignHeight="450"    d:DesignWidth="800"    mc:Ignorable="d">    <StackPanel Orientation="Vertical" Spacing="5">        <StackPanel HorizontalAlignment="Left" Orientation="Vertical">            <TextBlock>Меню редактирования учителей:</TextBlock>            <StackPanel Orientation="Horizontal">                <Button                    Margin="0,0,0,5"                    Command="{Binding AddNewTeacher}"                    Content="Добавить учителя" />                <Button                    Margin="20,0,0,5"                    Content="Продолжить"                    IsEnabled="{Binding IsEnabledContinue}" />            </StackPanel>            <DataGrid                Width="700"                Height="700"                AutoGenerateColumns="True"                Items="{Binding Teachers}"                SelectedIndex="{Binding DataGridSelectedIndex}">                <DataGrid.ContextMenu>                    <ContextMenu>                        <MenuItem Command="{Binding DeleteTeacher}" Header="Delete">                            <MenuItem.Icon>                                <Image Source="/Assets/trashcan.png" />                            </MenuItem.Icon>                        </MenuItem>                    </ContextMenu>                </DataGrid.ContextMenu>                <DataGrid.Columns>                    <DataGridTemplateColumn>                        <DataTemplate>                            <ComboBox Items="{Binding Disciplines, RelativeSource={RelativeSource AncestorType={x:Type vm:TeacherEditingMenuViewModel}}}" />                        </DataTemplate>                    </DataGridTemplateColumn>                </DataGrid.Columns>            </DataGrid>        </StackPanel>    </StackPanel>    <Design.DataContext>        <vm:TeacherEditingMenuViewModel />    </Design.DataContext></UserControl>