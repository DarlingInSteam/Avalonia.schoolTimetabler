<UserControl    x:Class="SchoolTimetabler.Views.SetPassword"    xmlns="https://github.com/avaloniaui"    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    d:DesignHeight="450"    d:DesignWidth="800"    mc:Ignorable="d">    <Grid>        <StackPanel            Margin="30"            HorizontalAlignment="Center"            VerticalAlignment="Top"            Orientation="Vertical">            <TextBlock>                Данное окно отвечает за задание пароля.            </TextBlock>            <TextBlock>Если вы не хотите устанавливать пароль,</TextBlock>            <TextBlock>то нажмите подтвердить, не вводя его.</TextBlock>        </StackPanel>        <StackPanel            HorizontalAlignment="Center"            VerticalAlignment="Center"            IsVisible="{Binding IsVisibleSetPassword}"            Spacing="15">            <Label HorizontalAlignment="Center">Пароль:</Label>            <TextBox                HorizontalAlignment="Center"                PasswordChar="*"                Text="{Binding Password}"                Watermark="Пароль" />            <TextBox                HorizontalAlignment="Center"                PasswordChar="*"                Text="{Binding PasswordTwo}"                Watermark="Повторите пароль" />            <Button                HorizontalAlignment="Center"                Command="{Binding Confirm}"                Content="Подтвердить" />        </StackPanel>        <StackPanel            HorizontalAlignment="Center"            VerticalAlignment="Center"            IsVisible="{Binding IsVisibleInputPassword}"            Spacing="15">            <TextBlock HorizontalAlignment="Center">У вас уже есть пароль.</TextBlock>            <TextBlock HorizontalAlignment="Center">Если хотите его поменять, то введите существующий.</TextBlock>            <TextBox                HorizontalAlignment="Center"                PasswordChar="*"                Text="{Binding Password}"                Watermark="Пароль" />            <Button                HorizontalAlignment="Center"                Command="{Binding ConfirmEqualPassword}"                Content="Подтвердить" />        </StackPanel>    </Grid></UserControl>